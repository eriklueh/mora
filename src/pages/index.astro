---
import BaseLayout from '@layouts/BaseLayout.astro';
// TODO(sanity): replace this stub with fetchBook() once content is seeded.
// import { sanity } from '@lib/sanity';
// import { BOOK_QUERY } from '@lib/queries';
// const book = await sanity.fetch(BOOK_QUERY);
---

<BaseLayout
  title="Mora — próximamente"
  description="Mora, el ebook. Migrando de HTML estático a Astro + Sanity."
>
  <section class="py-20 text-center">
    <h1 class="text-5xl font-bold tracking-tight">Mora</h1>
    <p class="mt-4 text-lg text-[color:var(--color-muted)]">
      El ebook se está migrando a una nueva casa.
    </p>
    <p class="mt-2 text-sm text-[color:var(--color-muted)]">Próximamente.</p>
  </section>
</BaseLayout>
